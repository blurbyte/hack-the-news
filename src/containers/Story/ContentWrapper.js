import styled from 'styled-components';

import RegularContentWrapper from '../../components/ContentWrapper';

const ContentWrapper = styled(RegularContentWrapper)`
  padding-bottom: 8rem;
`;

export default ContentWrapper;
