import styled from 'styled-components';
import media from '../../styles/media';

const Details = styled.div`
  display: flex;
  align-items: center;
  margin: 2.4rem 0 2.2rem 0;
  ${media.phone`margin: 1.2rem 0 1rem 0;`}
`;

export default Details;
