import React from 'react';

import Wrapper from './Wrapper';
import Button from '../Button';

const Footer = () => {
  return (
    <Wrapper>
      <Button>Next Random Story</Button>
    </Wrapper>
  );
};

export default Footer;
